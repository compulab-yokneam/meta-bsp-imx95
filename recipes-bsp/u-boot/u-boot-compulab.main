DESCRIPTION = "CompuLab i.MX95 U-Boot"
require recipes-bsp/u-boot/u-boot.inc

PROVIDES += "u-boot"
DEPENDS:append = " dtc-native"

LICENSE = "GPLv2+"
LIC_FILES_CHKSUM = "file://Licenses/gpl-2.0.txt;md5=b234ee4d69f5fce4486a80fdaf4a4263"

require u-boot-compulab_${PV}.inc
require u-boot-compulab/imx95.inc

DEPENDS += "flex-native bison-native bc-native dtc-native gnutls-native"

inherit fsl-u-boot-localversion

LOCALVERSION = "-compulab-${CL_RELEASE}"

BOOT_TOOLS = "imx-boot-tools"

do_compile:prepend () {
    export SOURCE_DATE_EPOCH=$(date +%s)
}

require u-boot-compulab-env.inc

PACKAGE_ARCH = "${MACHINE_ARCH}"
