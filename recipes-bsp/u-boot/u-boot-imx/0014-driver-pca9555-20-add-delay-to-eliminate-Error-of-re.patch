From 7de8b2e5a89f0cfc30aebc3267cc95b17b8607ba Mon Sep 17 00:00:00 2001
From: Valentin Raevsky <valentin@compulab.co.il>
Date: Wed, 24 Jul 2024 12:47:32 +0300
Subject: [PATCH 14/17] driver: pca9555@20: add delay to eliminate Error of
 reading output register

Signed-off-by: Valentin Raevsky <valentin@compulab.co.il>
---
 drivers/gpio/pca953x_gpio.c | 8 ++++++--
 1 file changed, 6 insertions(+), 2 deletions(-)

diff --git a/drivers/gpio/pca953x_gpio.c b/drivers/gpio/pca953x_gpio.c
index b0c66d18317..db9c3477f70 100644
--- a/drivers/gpio/pca953x_gpio.c
+++ b/drivers/gpio/pca953x_gpio.c
@@ -337,8 +337,12 @@ static int pca953x_probe(struct udevice *dev)
 
 	ret = pca953x_read_regs(dev, info->regs->output, info->reg_output);
 	if (ret) {
-		dev_err(dev, "Error reading output register\n");
-		return ret;
+		udelay(1000000);
+		ret = pca953x_read_regs(dev, PCA953X_DIRECTION, info->reg_direction);
+		if (ret) {
+			dev_err(dev, "Error reading output register\n");
+			return ret;
+		}
 	}
 
 	ret = pca953x_read_regs(dev, PCA953X_DIRECTION, info->reg_direction);
-- 
2.34.1

